import 'package:flutter/material.dart';

class RedPageVU extends StatelessWidget {
  const RedPageVU({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
